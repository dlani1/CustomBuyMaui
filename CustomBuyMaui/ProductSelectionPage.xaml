<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:CustomBuyMaui"  x:Class="CustomBuyMaui.ProductSelectionPage"
    Title="Selecciona tu producto">

    <Grid RowDefinitions="Auto,*" Padding="20">

        <Label 
            Text="Selecciona tu producto"
            FontSize="22"
            FontAttributes="Bold"
            HorizontalOptions="Center"
            Margin="0,10,0,10"/>

        <Grid Grid.Row="1" VerticalOptions="Center">

            <CarouselView 
                x:Name="MyCarousel"
                ItemsSource="{Binding Products}"
                IsSwipeEnabled="True"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                WidthRequest="300"
                HeightRequest="400">

                <CarouselView.ItemTemplate>
                    <DataTemplate>
                        <Frame 
                            HasShadow="True"
                            CornerRadius="15"
                            Padding="15"
                            BackgroundColor="#F5F5F5"
                            BorderColor="#DDDDDD"
                            WidthRequest="300"
                            HeightRequest="380"
                            HorizontalOptions="Center"
                            VerticalOptions="Center">

                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer 
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type local:ProductSelectionPage}}, Path=OnProductTapped}" 
                                    CommandParameter="{Binding}" />
                            </Frame.GestureRecognizers>

                            <VerticalStackLayout Spacing="10" HorizontalOptions="Center">
                                <Image 
                                    Source="{Binding ImageSource}" 
                                    WidthRequest="220" 
                                    HeightRequest="220" 
                                    Aspect="AspectFit"/>
                                <Label 
                                    Text="{Binding Title}" 
                                    FontAttributes="Bold"
                                    FontSize="18"
                                    HorizontalOptions="Center"/>
                                <Label 
                                    Text="{Binding Description}" 
                                    FontSize="14"
                                    HorizontalOptions="Center"
                                    HorizontalTextAlignment="Center"
                                    TextColor="#444"/>
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CarouselView.ItemTemplate>
            </CarouselView>
            

        </Grid>
    </Grid>
</ContentPage>